package com.hsuhau.hard.service.impl;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

@Service
public class FindMinImpl implements FindMin {
    private static final Logger LOGGER = LoggerFactory.getLogger(FindMinImpl.class);

    @Override
    public int finMin(int[] mums) {
        return 0;
    }
}
